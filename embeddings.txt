Отлично.
Без этого твой MCP будет просто «реестром».
С этим — он становится **мозгом юридической системы**.

Ниже — **как правильно добавить RAG-слой к судебной БД**, не разрушая юридическую точность.

---

# 1. Принцип

В юридическом AI нельзя делать:

> «один embedding на весь документ»

Потому что суд — это:

* факты
* аргументы
* выводы
* резолютивная часть
  — всё это имеет разный вес.

---

# 2. Добавляем Semantic Layer

```
DocumentVersion
        ↓
DocumentSection
        ↓
EmbeddingChunk
```

---

# 3. Новые таблицы

## 📄 DocumentSection

```
DocumentSection
---------------
id
document_version_id
section_type
order_index
text
```

`section_type`:

* FACTS
* CLAIMS
* ARGUMENTS
* LAW_REFERENCES
* COURT_REASONING
* DECISION
* AMOUNTS

Это позволяет спрашивать:

> «Покажи похожие аргументы суда»
> а не просто «похожие документы».

---

## 🧩 EmbeddingChunk

```
EmbeddingChunk
--------------
id
section_id
chunk_index
text
embedding_vector
token_count
```

Один section → 1–N chunks.

---

# 4. Связь с сущностями

```
EmbeddingEntityLink
-------------------
embedding_chunk_id
entity_type
entity_id
```

Например:

* этот абзац → судья X
* этот абзац → статья 625
* этот абзац → компания Rozetka

Это даёт:

> поиск не просто по смыслу, а по **юридическим объектам**.

---

# 5. Как MCP использует это

Когда LLM спрашивает:

> «Найди похожие дела, где потребитель выиграл у маркетплейса»

MCP делает:

1. Векторный поиск по `DECISION` + `COURT_REASONING`
2. Фильтрация по `DecisionOutcome = won`
3. Фильтрация по `Party.type = company`
4. Возвращает не PDF, а:

   * case_id
   * релевантные абзацы
   * ссылки на версии документов

---

# 6. Почему это в 10× лучше обычного RAG

Обычный RAG:

> “вот похожий текст”

Твой RAG:

> “вот похожий юридический паттерн”

Это критично:

* для предсказаний
* для шаблонов исков
* для анализа шансов

---

# 7. Это делает систему обучаемой

Когда выходит новое решение суда, MCP:

* парсит
* добавляет sections
* строит embeddings
* обновляет similarity graph

Система **эволюционирует вместе с судебной практикой.**

---

Если хочешь, дальше можем:

* разобрать пайплайн генерации embeddings
* или как LLM через MCP выбирает нужные sections
* или как поверх этого делается “вероятность выигрыша дела”
